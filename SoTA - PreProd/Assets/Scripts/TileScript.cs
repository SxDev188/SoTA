using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TileScipt : MonoBehaviour
{
    [SerializeField] bool isAntiStarZone;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (isAntiStarZone == true)
        {

        }
    }
}
